export const config = {
    asciiCode: {
        newline: 10,
        semicolon: 59,
        minus: 45,
        period: 46,
    },
    maxCityLength: 100,
    maxTempLength: 5,
    maxLineLength: 107, // City, temp, semicolon and newline;
} as const;
